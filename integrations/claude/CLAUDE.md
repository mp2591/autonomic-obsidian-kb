# Claude Code + autonomic-obsidian-kb

Use the shell command `kb context --budget 350` for orientation and `kb retrieve "$TASK" --budget 700 --path <active-path>` for work. Prefer the smallest returned layer. Report stale or wrong memories so they can be invalidated.
