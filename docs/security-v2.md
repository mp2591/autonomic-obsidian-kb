# Security v2 compatibility notice

The V2 executable-validator and bypassable-policy description is superseded. V3 removes all note-defined command execution, enforces MCP schemas and read policy, rejects secrets before persistence, and binds evidence identity to provenance fields. See [current security](security.md) and [V3 implementation](v3-implementation.md). Git history retains the original review baseline; it is not the current security contract.
